# ไฟล์ตัวอย่างสำหรับตั้งค่า Username และ Password
# คัดลอกไฟล์นี้เป็น secrets.toml และแก้ไขค่าให้ถูกต้อง
# อย่า commit ไฟล์ secrets.toml ขึ้น Git!

[auth]
username = "admin"
password_hash = "your_password_hash_here"

# วิธีสร้าง password_hash:
# 1. เปิด Python และรัน:
#    import hashlib
#    hashlib.sha256("your_password".encode()).hexdigest()
# 2. คัดลอก hash ที่ได้มาใส่ใน password_hash ด้านบน
