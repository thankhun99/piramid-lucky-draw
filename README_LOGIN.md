# 🔐 ระบบความปลอดภัย Login

## การตั้งค่า Username และ Password

### วิธีที่ 1: ใช้ Streamlit Secrets (แนะนำสำหรับ Deploy บน Streamlit Cloud)

1. ไปที่ Streamlit Cloud → Settings → Secrets
2. เพิ่มข้อมูลต่อไปนี้:

```toml
[auth]
username = "admin"
password_hash = "your_password_hash_here"
```

**วิธีสร้าง password_hash:**
```python
import hashlib
password = "รหัสผ่านที่ต้องการ"
password_hash = hashlib.sha256(password.encode()).hexdigest()
print(password_hash)
```

### วิธีที่ 2: ใช้ไฟล์ secrets.toml (สำหรับรันในเครื่อง)

1. สร้างโฟลเดอร์ `.streamlit` ในโปรเจกต์ (ถ้ายังไม่มี)
2. คัดลอกไฟล์ `.streamlit/secrets.toml.example` เป็น `.streamlit/secrets.toml`
3. แก้ไขค่า username และ password_hash ในไฟล์ `secrets.toml`

**⚠️ หมายเหตุ:** ไฟล์ `secrets.toml` จะถูก ignore โดย `.gitignore` แล้ว อย่า commit ไฟล์นี้ขึ้น Git!

### ค่า Default (สำหรับทดสอบ)

- **Username:** `admin`
- **Password:** `Piramid2026!`

**⚠️ ควรเปลี่ยนค่า default ก่อน deploy จริง!**

## การใช้งาน

1. เมื่อเปิดเว็บ จะแสดงหน้า Login
2. กรอก Username และ Password
3. กด "เข้าสู่ระบบ"
4. หลังจาก login สำเร็จ จะสามารถใช้งานระบบได้ตามปกติ
5. กดปุ่ม "ออกจากระบบ" ใน Sidebar เพื่อ logout

## ความปลอดภัย

- รหัสผ่านถูกเก็บในรูปแบบ Hash (SHA-256)
- ใช้ Streamlit Session State เพื่อจัดการสถานะ login
- รองรับการตั้งค่าผ่าน Streamlit Secrets สำหรับความปลอดภัยสูงสุด
